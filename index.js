// add solution here
function theBeatlesPlay(musician instrument){
  var strings = []
  for (i = 0; i > 10i++);
  if (i === 0)
    array.push("${musicians}""plays""${instrument}");
    return theBeatlesPlay
}